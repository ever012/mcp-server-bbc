// Flow básico de registro para ejemplo
// Este flow se dispara cuando se llama a bot.dispatch('EVENT_REGISTER', {...})

export const registerFlow = {
  keyword: "EVENT_REGISTER",
  // Este es un placeholder - en la implementación real se usaría addKeyword de BuilderBot
  // pero como solo lo necesitamos para disparar, lo definimos de forma mínima
};

